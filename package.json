{
  "name": "fusuma-hello",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "scripts": {
    "start": "fusuma start",
    "start:prod": "fusuma start-prod",
    "build": "fusuma build",
    "deploy": "npm run build && fusuma deploy",
    "pdf": "npm run build && fusuma pdf",
    "lint": "eslint \"slides/*.md\" --fix"
  },
  "devDependencies": {
    "eslint": "^8.22.0",
    "fusuma": "^2.8.4"
  }
}
