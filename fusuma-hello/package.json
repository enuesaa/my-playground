{
  "name": "fusuma-hello",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "scripts": {
    "start": "fusuma start",
    "start:prod": "fusuma start-prod",
    "build": "fusuma build",
    "deploy": "npm run build && fusuma deploy",
    "pdf": "npm run build && fusuma pdf"
  },
  "devDependencies": {
    "fusuma": "^2.8.4"
  }
}
